# Backend

Clean FastAPI and MongoDB application. The legacy backend in `old/` must not be imported. Start with configuration, database lifecycle, health checks, and shared schemas; add component inference modules only after their artifacts and contracts are validated.
