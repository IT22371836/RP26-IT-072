# Frontend

Clean React and TypeScript application. The legacy frontend in `old/` must not be copied or imported. Scaffold the application here after the shared pipeline and authentication contracts are agreed.
